# Disposable Hermes deployment and test workflow

<!-- markdownlint-disable MD013 -->

Record state: COMPLETE — real service smoke G05 PASS; live gateway G08 PASS; final G07 PASS

## Approved baseline — preserve after approval

- **Intended outcome:** Make Hermes changes quick to test with synthetic services in containers and with fresh, disposable Fedora VMs for boot, systemd, SELinux, networking, deployment, and recovery. Use the same deployment implementation and pinned Hermes artifact that would later be considered for production.
- **Approved design:** Keep the gateway and isolated offline worker needed to keep provider and Telegram credentials out of the tool environment. Replace the active TPM/LUKS/manual-install path with Fedora Cloud provisioning, unencrypted disposable disks, rootless Podman, and verified key-only SSH. Use `qemu:///session` and `passt` if a real VM run confirms they work on this host. Provide one routine test entrypoint and one VM integration entrypoint, with status and cleanup operations.
- **Credential decisions:** Synthetic credentials and mocked services are the default. A separately selected live smoke test uses dedicated revocable test credentials entered once into restricted host-only files outside Git, then consumed unattended. Maximum per live run: two DeepSeek requests and two outbound Telegram messages. Require a verified $1 provider-side cap if supported before enabling paid calls. Treat the Telegram user ID as sensitive authorization configuration. File modes do not protect against host administrators, a compromised credential-consuming process, or offline access to an unencrypted disk.
- **Cleanup decision:** Retire `lab-hermes-deepseek-e2e-r1` and `lab-hermes-manual-r1` and their confirmed owned disks, containers, and temporary grants after exact identity and ownership checks. Preserve unrelated resources and byte-bound historical evidence.
- **Acceptance:** Fresh unattended deployment; safe rerun; quick change and retest; automatic startup after reboot; partial-failure recovery; repeatable cleanup; synthetic credential isolation and Telegram allow/reject tests; separate live-smoke result; cold and warm timings and bottlenecks; repository offline and relevant runtime checks; concise canonical documentation and exact commands. Distinguish mocked results from live and production acceptance.
- **Authorization:** The user's `deplyment approved` approves the short five-step implementation plan presented earlier in this conversation, including lab deployment and the named obsolete-resource cleanup. The attached request explicitly excludes production deployment, commits, pushes, merges, and PR changes. No reusable sudo password may be stored or relied on.
- **Record owner:** This file is the one canonical execution record for this change. The 2026-09-23 DeepSeek VM and 2026-09-24 Gate 2 records remain historical; do not rewrite their evidence.

## Starting state and provenance

- Repository: branch `codex/hermes-deepseek-e2e-r1`, HEAD `ef3fc9c4efe1adbf7631ce96be203871e9c6c75f`. At approval: 24 modified tracked paths and 19 untracked paths. `git diff --binary | sha256sum` was `c5a3602fa6a78183720c7632aa5dc9ffb078c94aec87bfb4c8fcb24facd87c9f`; `git status --porcelain=v1 -z | sha256sum` was `f74e242898af07188b6436521141bd0d855cc9a83d4189bed9b45b18c33034c6`. These identify the initial tracked diff and path status, not the bytes of untracked files.
- Reusable work: the manual profile's pinned gateway image, rootless gateway/worker Quadlets, worker transport, profile contract, native DeepSeek model selection, and synthetic offline tests. The 2026-09-25 manifest records Hermes v0.21.5 by digest; revalidate the artifact before deployment.
- Obsolete active path: M01 installs TPM/cryptsetup and requires `/home/hermes` as a separate mount; M02–M05 contain the same mount assumption. The TPM/LUKS boot, fixed-operation grant, and interactive installation helpers do not serve the new lab workflow. Preserve historical records while removing them from current guidance and routine execution.
- Prior read-only libvirt inventory found `lab-hermes-deepseek-e2e-r1` running, UUID `4349a2bc-67b6-42b7-a6ef-5aeee5ee4009`, disk `/var/lib/libvirt/images/lab-hermes-deepseek-e2e-r1/disk.qcow2`; `lab-hermes-manual-r1` shut off, UUID `d39e5ca7-aded-457c-b01d-a1817a4725a8`, disk `/var/lib/libvirt/images/lab-hermes-manual-r1/disk.qcow2`. Reconfirm all identities, attached volumes, grant ownership, and credential absence before deletion.
- The existing offline gate took 84.94 seconds before implementation. Its component suites and working-tree secret scan passed, but the aggregate gate failed because the migration manifest was stale (379 current payload rows versus 368 recorded). This is not an accepted final gate.
- `virt-install --print-xml --dry-run` against `qemu:///session` accepted `passt` with localhost TCP SSH forwarding and left no domain defined. A real guest boot/network/SSH test remains required.

## Tasks and gates

| Task ID | Outcome and dependencies | Task progress | Required gates | Exact next action |
| --- | --- | --- | --- | --- |
| T01 | Inventory dirty work and exact lab resources; preserve history; none | ✅ DONE | G01 | Inventory complete; use the exact identities for T06 cleanup |
| T02 | Shared unencrypted deployment path for gateway and worker; T01 | ✅ DONE | G02 | Fresh/rootless synthetic VM and unchanged rerun passed; monitor the one extra first-rerun convergence below |
| T03 | Fast routine container test entrypoint with synthetic DeepSeek and Telegram; T02 | ✅ DONE | G03 | Loopback mock HTTP, allow/reject and fail-closed tests passed in a network-disabled pinned image |
| T04 | Fresh disposable VM integration entrypoint and cached base; T02 | ✅ DONE | G04 | Fresh boot/deploy, reboot, fault recovery and corrected guarded cleanup passed; domain and instance are absent |
| T05 | Private live-smoke path and bounds; T02–T04 | ✅ DONE | G05 | New private key passed one real DeepSeek completion and one Telegram outbound API message |
| T06 | Retire obsolete owned lab resources; T01 | ✅ DONE | G06 | Root cleanup, independent absence checks and private no-op rerun passed |
| T07 | Reconcile current docs, repository gates and acceptance; T02–T06, T08 | ✅ DONE | G07 | Final manifest, offline, lint, diff and cleanup checks passed; no required action remains in scope |
| T08 | Real Hermes gateway to DeepSeek and Telegram round trip; T02, T04, T05 | ✅ DONE | G08 | Core round trip passed under the user's accepted criterion; exact-output deviation retained below |

| Gate ID | Task ID | Method and expected result | Gate status | Latest evidence |
| --- | --- | --- | --- | --- |
| G01 | T01 | Read-only Git and host inventory; exact target ownership, unrelated work and historical evidence identified | ✅ PASS | 2026-09-25 baseline libvirt XML, installed grant, Podman cache and Git status below (historical inventory) |
| G02 | T02 | Synthetic deploy on fresh target and safe rerun; same pinned artifact, unencrypted storage, rootless gateway/worker, SELinux enforcing, no worker provider/bot credentials | ✅ PASS | 2026-09-25 fresh Fedora Cloud VM, rootless service/worker inspect, SSH round trip and eventual `UNCHANGED` rerun |
| G03 | T03 | Routine entrypoint checks mock API behavior, credential handling, Telegram allowlist rejection, no external calls, and measured repeat time | ✅ PASS | Pinned-image network-disabled loopback DeepSeek/Telegram mock HTTP and negative authorization passed |
| G04 | T04 | Fresh VM deploy, key/host authentication, reboot autostart, changed-source retest, injected failure/recovery, cleanup rerun, and cold/warm timing | ✅ PASS | Current fresh boot 13.76 s, deploy 56.60 s, corrected double-clean and independent absence; earlier reboot, warm and fault-recovery evidence below |
| G05 | T05 | Private source and live preflight prove limits; explicit smoke checks provider and Telegram separately without secrets in arguments, logs, image, snapshot or worker | ✅ PASS | New key: one bounded DeepSeek chat completion and one Telegram `sendMessage` passed; script performed no retries |
| G06 | T06 | Exact old resource teardown, absence checks and repeated cleanup; preserve immutable evidence | ✅ PASS | Corrected private root cleanup and no-op rerun returned `RETIRE=PASS`; independent read-only domain and path checks passed |
| G07 | T07 | Prescribed `toolbox/run-offline-checks.sh`, relevant lint/links and runtime checks all pass on final inputs; results classified accurately | ✅ PASS | Mode-aware 392-row manifest, 510 reviewed Python tests, 140 ShellSpec examples, 220 guide/deployment checks, secret scan, check-only lint and `git diff --check`; final-input rerun below |
| G08 | T08 | Authorized private Telegram inbound produces a DeepSeek-backed reply containing the test marker through the pinned Hermes gateway; worker stays offline; live credentials cleared afterward | ✅ PASS | User-reported reply began `HERMES_LIVE_OK` and included onboarding text; user accepted the core round trip. Live tmpfs and VM were cleared |

## Evidence history

### G01 — current inventory, 2026-09-25 America/Bogota

- Method and working directory: `git status --short --branch`, `git diff --stat`, and source inspection in this repository; libvirt read-only XML for only `lab-hermes-deepseek-e2e-r1` and `lab-hermes-manual-r1`; `sudo -n -l`; host-user `podman images` and `podman ps -a`.
- Expected result: The dirty work and cleanup targets are distinguished from other repositories, development containers and immutable historical evidence.
- Actual result and gate status: ✅ PASS. The branch has 24 modified tracked and 19 untracked files at baseline. Reusable: pinned v0.21.5 gateway manifest/image, DeepSeek model selection, gateway/worker transport, profile contract, synthetic tests. Obsolete for the new workflow: TPM/LUKS and separate-mount helpers, retained encrypted VM fixture, fixed-operation grants and old manual VM fixture. Keep the historical plans and evidence unchanged. The full content-level diff still needs review before targeted discard of any old code.
- Checked runtime identities: DeepSeek UUID `4349a2bc-67b6-42b7-a6ef-5aeee5ee4009`, MAC `52:54:00:83:bf:4b`, `fvh-nat`, running, one attached qcow2 at `/var/lib/libvirt/images/lab-hermes-deepseek-e2e-r1/disk.qcow2`; manual UUID `d39e5ca7-aded-457c-b01d-a1817a4725a8`, MAC `52:54:00:94:58:36`, `fvh-nat`, shut off, qcow2 at `/var/lib/libvirt/images/lab-hermes-manual-r1/disk.qcow2` plus its two named boot ISOs. Neither domain has a libvirt snapshot. The DeepSeek host NOPASSWD rule still lists only five exact fixed commands; installed `access.py` is byte-identical to its repository source (`e9fffae62535e3714c4d406f3359c41218d9e6d9411e98bc84ad8c82e638fd39`). The prior manual host grant is absent from `sudo -n -l`. Host-user Podman has cached gateway digests and development Toolboxes, but no Hermes lab container. Do not delete those shared image caches or Toolboxes.
- Limit: The old VM disks were identified by libvirt attachments and exact prior plan identities; absence of credentials inside an inaccessible guest is not established by this inventory. The old DeepSeek plan records no application or live-provider deployment. Deletion remains confined to the two named obsolete fixtures and requires post-action absence checks.

### G06 — first teardown attempt, 2026-09-25 America/Bogota

- Method and working directory: The installed exact-VM `sudo -n /usr/bin/python3 -I /var/usrlocal/libexec/hermes-deepseek-test/access.py host teardown` from this repository, after a matching fixed status check and byte comparison of the root-owned installed code to the reviewed repository source.
- Expected result: Remove the named guest grant and SSH entry, shut down `lab-hermes-deepseek-e2e-r1`, then remove the host grant and temporary key state.
- Actual result and gate status: ❌ FAIL. The guest-agent-executed cleanup returned `PermissionError: [Errno 13]` while unlinking `/etc/sudoers.d/90-hermes-deepseek-test-guest`. The wrapper stopped before host cleanup. A repeat fixed status returned `domain=lab-hermes-deepseek-e2e-r1 state=running`; `sudo -n -l` still lists the five named host operations. Guest grant removal is unverified. Do not rerun the same guest-agent deletion method unchanged.
- Checked inputs: The two exact domain identities and the installed host grant; no application credentials were read or displayed. The failure is a guest SELinux execution-boundary observation, not a reason to disable SELinux.

### G06 — bounded alternative shutdown, 2026-09-25 America/Bogota

- Method: An unprivileged `virsh -c qemu:///system destroy lab-hermes-deepseek-e2e-r1` attempt timed out connecting; immediate read-only libvirt state still showed the exact UUID running. The already-installed exact `host shutdown` operation was then used once, followed by its fixed `host status` readback.
- Expected result: Stop only the obsolete DeepSeek domain while retaining all other VMs.
- Actual result: The fixed shutdown returned a request acknowledgment, and fixed status returned `domain=lab-hermes-deepseek-e2e-r1 state=shut off`. This does **not** satisfy G06: the host grant and named disks remain, and guest grant deletion inside the disk was not proved. A separate writable libvirt connection as the unprivileged account returned `authentication failed: access denied by policy`; do not repeat it unchanged. Use one private host authorization for the exact domain and grant cleanup, or another reviewed bounded host path. No sudo password was stored or submitted.

### G07 — pre-implementation baseline, 2026-09-25 America/Bogota

- Method and working directory: `toolbox run --container dev-infra-hermes bash -c 'cd ~/code/repos/hermes-fedora-host && bash toolbox/run-offline-checks.sh'`; timed from the host repository root.
- Expected result: Every required offline component passes.
- Actual result and gate status: ❌ FAIL in 84.94 seconds. Manual, review, backup, configure, contract, M01, clean-install, guide, ShellSpec, reviewed Python, and secret-scan components passed; migration manifest verification failed on stale destination hashes/rows and missing new files.
- Checked inputs: The dirty/untracked worktree summarized above, as seen by the Toolbox. This baseline does not test runtime, paid provider, Telegram delivery, SELinux behavior, or VM lifecycle.

### G02/G04 — disposable Fedora Cloud runtime, 2026-09-25 America/Bogota

- Method: `vm/hermes-disposable.py verify-base|run|deploy|reboot|fault-test|clean` on the approved host's `qemu:///session` connection. The Fedora 44 1.7 Cloud Generic qcow2 SHA-256 `28680fe5b371a5a82ebf43a31926e086a168e59949d03969c5093e7071f90b7f` matched its signed checksum; `gpgv` accepted signer `36F612DCF27F7D1A48A835E4DBFCF71C6D9F90A6`. The host libosinfo catalog lacks `fedora44`; `fedora43` metadata was used for the actual Fedora 44 image.
- Expected result: Fresh unencrypted overlay and generated key-only, host-key-pinned SSH; enforcing SELinux, rootless gateway/worker with the same pinned manifest image, no worker network or provider/bot state; reboot startup, safe rerun, repair and repeatable exact cleanup.
- Actual result: ✅ PASS within the synthetic lab scope. Fresh SSH reached in 14.21 s, preparation/deploy in 59.32 s, reboot and both services returned in 15.12 s. `STATUS=PASS` verified a gateway-to-worker SSH round trip, worker `Network=none`, and absence of DeepSeek/Telegram variables and gateway private mounts in the worker. A missing-key SSH attempt was rejected. The first unchanged warm run reported `CHANGED` in 5.51 s because configuration converged once more; the next run reported `UNCHANGED` in 1.76 s. A deliberately stopped worker was restarted by `deploy` with `RECOVERY=PASS`. Exact cleanup and its repeat both reported PASS, and a new fresh VM was created from the cached base. The changed profile verifier source was transferred and passed on the running VM.
- Earlier failures retained: unsupported `fedora44` libosinfo name, cloud-init schema rejecting `ssh_genkeytypes: []`, rootless `runuser` inheriting an inaccessible current directory, host mode-0600 source preventing guest image build, test SSH defaulting to user `hermes` instead of `worker`, and worker home ownership changing inside the user namespace. Each was corrected and the relevant step rerun. The first VM's cloud-init showed a degraded schema warning; it was cleaned and a fresh VM with corrected seed passed `cloud-init status --wait`.
- Limit: The cached base contains no application layers. Fresh guest DNF/package installation and gateway pull/worker build dominate the ~59 s deployment; warm deployments reuse the guest's image cache. No paid provider, real Telegram inbound, production host, TPM or LUKS assertion is implied.

### G03/G05 — synthetic tests and private live path, 2026-09-25 America/Bogota

- Method: `bash scripts/hermes/lab-test.sh test` with the pinned cached image and `--network none`; `tests/hermes-synthetic-smoke.py` serves loopback mock DeepSeek and Telegram endpoints, checks actual DeepSeek runtime resolution and Telegram callback allow/reject/fail-closed paths, and asserts exactly one POST to each mock. `tests/test_hermes_live_smoke.py` mocks the optional host live-smoke requests and checks at most one call per service, private file modes, and no secret text in the script's output.
- Actual result: ✅ G03 PASS. The network-disabled container printed `PASS: pinned-image DeepSeek/Telegram mock HTTP, allow/reject (synthetic, offline)`; the three live-path offline unit cases passed. No external service was contacted by these checks. The host-only interactive setup and direct API smoke script are implemented, but G05 is ⬜ NOT RUN because dedicated credentials were not entered. DeepSeek's public API docs did not establish a programmatic provider-side $1 cap; the private setup records an operator-verified or unavailable status without claiming the program set a cap. The direct API smoke, if later run, is still not Hermes gateway end-to-end or authenticated inbound Telegram acceptance.

### G06 — exact host cleanup authorization, 2026-09-25 America/Bogota

- Method: Review of `vm/retire-legacy-labs.py`, which preflights both UUIDs, MACs, `fvh-nat`, exact attachments, shut-off state, no snapshots, no other-domain storage sharing, per-VM NVRAM/TPM state, owned directory contents, and the exact DeepSeek sudoers hash before any deletion. The host showed both domains shut off and the targeted disk/media/NVRAM ownership matched the script's constraints. `pkexec /usr/bin/python3 -I vm/retire-legacy-labs.py --apply` was attempted once.
- Expected result: One private host authorization removes only the two obsolete owned domains, disks, named media and temporary host grant.
- Actual result and status: 🚫 BLOCKED. PolicyKit returned `Not authorized` before script execution; no resource was deleted by this method. The user was given the exact `sudo python3 -I vm/retire-legacy-labs.py --apply` private-terminal handoff. Do not claim absence until its output and independent read-only checks are received. The earlier guest-agent SELinux failure remains recorded above.

### G07 — offline and lint progression, 2026-09-25 America/Bogota

- Method: Prescribed `toolbox/run-offline-checks.sh` inside `dev-infra-hermes`, then `toolbox/run-check-only-lint.sh`, `git diff --check`, Ruff check, targeted Markdown lint and local-link validation. The full offline log is at `/tmp/hermes-disposable-offline.log`; lint output is at `/tmp/hermes-disposable-lint-final.log` on this workstation.
- Expected result: Required offline and non-writing lint gates pass on the complete final working tree without rewriting byte-bound historical records.
- Actual progression: The full offline run after manifest refresh passed 489 reviewed Python tests, 140 ShellSpec examples, all other shell suites, the full working-tree secret scan, and source-aware manifest verification at 388 rows. The check-only lint gate passed on all 101 shell files and 47 Markdown files (including ShellCheck at certifier and whole-tree severities, shfmt check-only, and markdownlint-cli2).
- Prior gate status: ✅ PASS on the committed inputs. The 2026-09-25 domain-discovery correction below makes this pass 🔁 STALE until the required gates rerun.

## Decisions and approved scope changes

| Date/time if known | Decision and reason | Approval evidence if required | Affected tasks/gates |
| --- | --- | --- | --- |
| 2026-09-25 America/Bogota | Use host-only restricted files for dedicated live test credentials after one private entry step | User answered the consolidated question | T05/G05 |
| 2026-09-25 America/Bogota | Use two provider requests, two outbound Telegram messages and a $1 provider-side cap if supported | User answered the consolidated question | T05/G05 |
| 2026-09-25 America/Bogota | Retire both obsolete Hermes test VMs after ownership checks | User answered the consolidated question | T01/T06/G01/G06 |

## First handoff (historical)

- **Current outcome:** G02–G04 passed for the synthetic disposable lab. The current operator guide, host-only private credential setup, and bounded direct API smoke are implemented. The fresh session VM `hermes-disposable-lab`, UUID `1e37d7e5-5ee4-41fc-8d2d-43f9eb1c6fae`, was read back at handoff as `DOMAIN=running` with both services active and the worker network disabled. Its ignored cache/overlay is under `.toolbox/hermes-disposable/`; it contains only synthetic application state and disposable SSH keys, no live provider or bot credentials. Keep it for a quick continuation or run the exact `python3 vm/hermes-disposable.py clean` when done.
- **Private cleanup gate:** G06 remains 🚫 BLOCKED until the operator's private-terminal result is received. PolicyKit denied this agent's attempt before the script ran. The user requested the command and was given `cd /var/home/aicloudopspecial/code/repos/hermes-fedora-host && sudo python3 -I vm/retire-legacy-labs.py --apply`; no resulting `RETIRE=` or `STOP:` line had arrived at this handoff. The script also pins the old VMs' UEFI NVRAM and TPM state; its current SHA-256 is `03b38b297e38c0123da6286012993729a130846ac088f162b620ee65d9a980b2`. If it reports PASS, independently use read-only `virsh --readonly -c qemu:///system` and exact path checks to prove both old domains, disks, named ISOs, NVRAM, temporary rule and host state are absent. A partial `STOP:` needs an exact-target recovery review, not a broadened delete.
- **Live gate:** G05 remains ⬜ NOT RUN. Dedicated DeepSeek/Telegram test credentials have not been provisioned in this session. The operator can privately run `python3 scripts/hermes/live-smoke.py setup` and report only `SETUP=PASS`; then run `smoke` within the fixed one-request/one-message limits. The optional direct API smoke still does not certify Hermes gateway end-to-end or an authenticated inbound Telegram message. Provider-side $1 cap support was not established from public docs; setup records either independently verified or unavailable status. Never place values in chat, Git, arguments or a VM seed.
- **Validation at handoff:** The required offline gate passed on its then-current inputs: 489 reviewed Python tests, 140 ShellSpec examples, all shell suites, whole-working-tree secret scan and 388-row source-aware manifest verification (`/tmp/hermes-disposable-offline.log`). Later edits make that result **STALE** for final-input acceptance. The full non-writing lint gate subsequently passed (`/tmp/hermes-disposable-lint-final.log`), including exact historical byte-preserving exclusions. Ruff check, focused three-case live-script unit test, the network-disabled loopback mock test, local link validation, `git diff --check`, and a formatted-source VM `DEPLOY=UNCHANGED` rerun passed. No real DeepSeek or Telegram calls were made.
- **Dirty-work identity:** Branch `codex/hermes-deepseek-e2e-r1`, HEAD `ef3fc9c4efe1adbf7631ce96be203871e9c6c75f`. Before this handoff edit, `git diff --binary | sha256sum` was `d844c872f0f6b183eef50df245f3aba2206126f2148172e1d9e2bc8e3451c703`; `git status --porcelain=v1 -z | sha256sum` was `d3d88b07eb4731b5a4fa9daafd0d23509d754ea3ee0d3ebda69e0323bcb7c2a2`. These change with the record/manifest and do not bind untracked file bytes. Preserve the 24 modified and 19 untracked starting paths identified above; new lab work is additional. No commit, push, PR change or production deployment occurred.
- **Resume in order:** (1) Capture the operator's exact cleanup result and verify G06 independently, or retain its blocker. (2) If dedicated credentials are privately available, run the separate bounded live smoke and record G05; keep Hermes gateway E2E and real inbound authorization explicitly unproven unless separately exercised. (3) Re-read `AGENTS.md` and this record, inspect Git status/diff, regenerate `docs/migration-manifest.txt`, run `toolbox/run-offline-checks.sh` in `dev-infra-hermes` and `toolbox/run-check-only-lint.sh`, then mark G07 from the **final** inputs. (4) Run `vm/hermes-disposable.py clean` twice when the lab VM is no longer needed, record absence, and reconcile all acceptance criteria. Do not rewrite the historical dated plans or evidence.
- **Final state:** ACTIVE handoff requested by the user on 2026-09-25; implementation and evidence remain in the worktree for continuation.

## Continuation on 2026-09-25 America/Bogota (earlier)

- **Starting state:** The worktree was clean at commit `fb369c3695ec94dc40735bc45b567242920ff93b`, with the branch tracking the same commit on `origin/codex/hermes-deepseek-e2e-r1`. This commit and its push were already present at resumption; this continuation made no commit, push, PR change or production deployment. The first handoff above is retained as historical status, including its then-stale gate and earlier Git identity.
- **G04 correction and current runtime:** An unprivileged sandbox `python3 vm/hermes-disposable.py status` printed `DOMAIN=ABSENT` even though read-only host libvirt listed `hermes-disposable-lab`. The libvirt connection had actually failed because the sandbox could not create its user-service lock. `domain_exists()` had treated any failed `domuuid` as absence, which could also let `clean` remove local instance files without reaching libvirt. It now uses a checked `virsh list --all --name`; a connection failure produces `STOP:` and preserves files. Three reviewed offline regressions cover present, absent and failed-connection cases. The actual host readback after the correction was `DOMAIN=running`, `STATUS=PASS worker=active gateway=active worker_network=none`. This correction does not change the earlier fresh-boot and repeat-clean evidence.
- **G05/G06 remain open:** `live-smoke.py status` returned `STOP: private host setup has not been completed`; no live call was made. Read-only `qemu:///system` listed both `lab-hermes-deepseek-e2e-r1` and `lab-hermes-manual-r1`, each `shut off`. The operator's private `RETIRE=` or `STOP:` result has not arrived; G06 remains BLOCKED, and no old VM deletion is claimed.
- **G07 current gate:** The manifest was regenerated to 389 rows after the correction. The prescribed offline gate passed all components on the corrected source and guide, including 492 reviewed Python tests, 140 ShellSpec examples, the full working-tree secret scan and manifest verification (`/tmp/hermes-disposable-offline-current.log`). `toolbox/run-check-only-lint.sh` passed (`/tmp/hermes-disposable-lint-current.log`); focused Ruff and `git diff --check` passed. This plan record is part of the manifest payload, so the manifest was refreshed again after this continuation entry and the full offline and check-only lint gates were rerun on that exact record before retaining G07 as PASS. T07 remains IN_PROGRESS because T05 and T06 are dependencies.
- **Exact next action:** Receive the private cleanup command's final `RETIRE=` or `STOP:` line and independently verify exact host absence before closing G06. If dedicated credentials are privately provisioned, run the bounded service smoke and record G05 separately. Once no further runtime testing is needed, run `python3 vm/hermes-disposable.py clean` twice through actual host libvirt, verify absence, then reconcile T07 and acceptance. Do not treat the sandbox's libvirt connection error as VM absence.

## Continuation on 2026-09-25 19:04 America/Bogota (earlier)

- **G04 repeatable cleanup:** The session marker and live `qemu:///session` domain both identified `hermes-disposable-lab` as UUID `1e37d7e5-5ee4-41fc-8d2d-43f9eb1c6fae`. Host-access `status` returned `DOMAIN=running` and both services active with `worker_network=none`. The guarded `python3 vm/hermes-disposable.py clean` returned `CLEAN=PASS` twice. Independent host libvirt listing was empty, the exact instance directory was absent, and the corrected host-access `status` returned `DOMAIN=ABSENT`. The verified Fedora image, signed checksum and Fedora keyring remain in the ignored cache for future fresh instances. This is a current cleanup result; the earlier first-VM repeat-clean evidence remains above.
- **Remaining gates:** Host-only live setup is still absent, so G05 remains NOT RUN. Read-only system libvirt still lists both obsolete shut-off VMs, so G06 remains BLOCKED pending the operator's private-terminal cleanup result and independent post-action absence checks. No live call, old-VM deletion, production deployment, commit, push or PR change occurred in this continuation.
- **G07 after this record edit:** Refresh the source-aware manifest and rerun the prescribed offline and check-only lint gates because this plan record is part of the manifest payload. Retain G07 as PASS only if the rerun succeeds on this exact record; preserve any failure instead of claiming final-input acceptance.
- **Exact next action:** Receive the operator's final `RETIRE=` or `STOP:` line from the private cleanup command, verify exact old-domain/disk/grant state, and record G06. When dedicated credentials are privately provisioned, run and separately record the bounded live smoke for G05; the direct API path still does not prove Hermes end-to-end or authenticated inbound Telegram. Recreate the disposable VM with `python3 vm/hermes-disposable.py run` only when another runtime check needs it.

## Continuation on 2026-09-25 19:10 America/Bogota (earlier)

- **G04 guarded cleanup correction:** Review found that a marked instance skipped the child-file allowlist before `shutil.rmtree`. The runner now checks exact permitted regular files and rejects symlinks and unexpected paths whether or not the ownership marker exists. A fourth reviewed offline regression leaves an unexpected `unrelated.txt` intact and verifies the refusal. Focused Python unit tests, Ruff and `git diff --check` passed before the runtime run.
- **G04 current runtime:** From the cached signed Fedora base, a new synthetic VM reached SSH in 13.76 seconds and deployed in 56.60 seconds. Its status reported both rootless services active with `worker_network=none`. The instance contained only the ten expected files. Corrected `clean` returned `CLEAN=PASS` twice; independent `qemu:///session` listing was empty, the instance directory was absent, and host-access `status` reported `DOMAIN=ABSENT`. The verified Fedora base cache remains for later fresh runs. Earlier reboot, warm-rerun and injected worker-recovery evidence remains applicable because this correction affects only cleanup validation.
- **G07 final-input check:** This record and the corrected runner/test are included in the source-aware migration manifest. Regenerate the manifest after this entry, then rerun the prescribed full offline gate and check-only lint. Retain G07 as PASS only if both pass on these inputs; the prior 492-test/389-row pass is stale after the added regression and this record edit.
- **Remaining gates and next action:** G05 is NOT RUN because no private live setup is present. G06 remains BLOCKED pending the operator's private `RETIRE=` or `STOP:` result; the two obsolete system VMs were still listed at the last read-only check. Receive that result, independently verify exact old-resource absence or review a bounded partial failure, then record G06. Run live smoke only after dedicated credentials are privately provisioned, and keep direct API acceptance separate from Hermes end-to-end and authenticated inbound Telegram.

## Continuation on 2026-09-25 19:34 America/Bogota (earlier)

- **G06 failed private attempt:** The first two noninteractive `sudo` invocations stopped before script execution because no terminal was available. The operator then ran the exact `sudo python3 -I vm/retire-legacy-labs.py --apply` in a private terminal; its root preflight returned `STOP: unexpected exact directory contents: /var/lib/libvirt/boot/lab-hermes-manual-r1`. The script performs all preflight checks before mutation, and a subsequent read-only libvirt listing still showed both approved domains defined. No deletion is claimed. G06 is FAIL until the corrected script is privately rerun and independently verified.
- **Exact boot-directory evidence and correction:** The operator's names-and-metadata-only listing showed six regular files: qemu-owned installer ISO (0644) and `ks.iso` (0640), root-owned Fedora CHECKSUM and `fedora.gpg` (0644), and root-owned `ks.cfg` and `guest-password.hash` (0600). Independent unsandboxed `stat` confirmed their owners, modes and sizes without reading contents; the root-owned directory is mode 0711. Repository fixture and kickstart generators account for all four additional files. The retirement script now pins exactly these six names and their owner/mode rules, rejects any other child or a changed directory mode, checks all six against other-domain storage references, removes only these exact files, and uses its full absence check after deletion. Three synthetic offline regression cases cover the six-file set, an extra file and a loosened password-hash mode, plus a changed directory mode; no live root `--check` or `--apply` has yet run on the corrected source.
- **Other inspected paths:** Read-only host metadata matched the script's pinned directory modes and file owners for both disks, both NVRAM files, the root-owned installed DeepSeek helper directory, and the user-owned temporary key directory. The root-owned host sudoers rule could not be read as the unprivileged account; the next private root preflight will check its mode, exact SHA-256 and `visudo` validity before any deletion. The two attached manual ISO paths were confirmed by read-only libvirt `domblklist`.
- **G07 after this record edit:** Regenerate the source-aware migration manifest, then rerun the prescribed full offline gate and check-only lint. Retain G07 as PASS only if both pass on the corrected script, new test, guide and this included plan record; preserve a failure otherwise.
- **Exact next action:** The operator runs `sudo python3 -I vm/retire-legacy-labs.py --apply` in the private host terminal using the corrected repository file and returns only the final `RETIRE=` or `STOP:` line. If PASS, independently prove the two domains, exact files, NVRAM and temporary grant absent; if STOP, diagnose that exact condition without broadening cleanup. G05 remains NOT RUN until dedicated private credentials are provisioned.

## Continuation on 2026-09-25 America/Bogota (retirement result)

- **G06 root result:** The operator ran the corrected `--check && --apply` command in a private host terminal and reported the final line `RETIRE=PASS both exact obsolete domains, owned files, and temporary host grant absent`. Because the commands were joined by `&&`, reaching `--apply` also indicates the root preflight exited successfully. The script's final `already_absent()` check covers both exact domains, both disk directories, the six manual boot files and their directory, both NVRAM files, the temporary sudoers rule, the installed host helper directory, and the temporary user state. The earlier refused attempt remains historical evidence above.
- **Independent read-only result:** An unsandboxed `virsh --readonly -c qemu:///system list --all --name` returned an empty domain list. Unprivileged path metadata checks reported the two exact disk directories, manual boot directory, both NVRAM files, installed helper directory and temporary user state absent. The unprivileged account cannot traverse `/etc/sudoers.d`, so the root script's post-action PASS is the evidence for the rule's absence. This independent check did not directly inspect libvirt's private TPM state; the root script used `virsh undefine --tpm` for each identity-pinned domain.
- **Rerun and final gate:** A private no-op `--apply` rerun was requested to satisfy the repeated-cleanup part of G06. Its result is recorded in the closure below. No production deployment, commit, push, merge or PR change was performed by this continuation.

## Closure on 2026-09-25 America/Bogota

- **G06 repeat result:** The operator's second private `sudo python3 -I vm/retire-legacy-labs.py --apply` returned `RETIRE=PASS exact obsolete resources already absent`. Together with the first root post-action PASS and independent read-only checks, this satisfies exact retirement and repeatable cleanup. The unprivileged check still cannot independently traverse `/etc/sudoers.d`; the root script's two PASS results cover that path. TPM state was removed through each identity-pinned `virsh undefine --tpm`, not by a separate direct directory inspection.
- **G05 optional live smoke:** The host-only credential setup and bounded direct API smoke path are complete, but no dedicated credentials were provided and no DeepSeek or Telegram live calls were made. G05 is therefore NOT RUN. Synthetic allow/reject and mocked API checks are PASS; they do not certify authenticated inbound Telegram, Hermes gateway end-to-end integration, production readiness or a provider-side spending cap.
- **G07 closure validation:** After the G06 no-op result was recorded, the source-aware manifest was regenerated to 390 rows; `toolbox/run-offline-checks.sh` passed 496 reviewed Python tests, 140 ShellSpec examples, all other shell suites, the full working-tree secret scan and manifest verification (`/tmp/hermes-disposable-offline-retirement-closure.log`). `toolbox/run-check-only-lint.sh` passed (`/tmp/hermes-disposable-lint-retirement-closure.log`), as did `git diff --check`. Refresh the manifest and rerun these gates after this final record edit so the recorded PASS still covers the exact delivered bytes. The measured fresh boot was 13.76 seconds and deployment 56.60 seconds; the earlier warm unchanged deploy was 1.76 seconds. The remaining main cost is building/provisioning the fresh VM rather than running the routine synthetic container checks.
- **Remaining production gates:** Provision dedicated revocable private test credentials if the optional service smoke is wanted, verify any available provider-side $1 cap, run the bounded provider/Telegram checks, and separately exercise Hermes gateway end-to-end and authenticated inbound Telegram if production acceptance is required. None of those is claimed here. The disposable VM is cleaned, the two obsolete VMs and temporary grant are retired, and shared verified base caches remain for new test instances. The implementation and record remain uncommitted in the current branch.

## Live integration extension on 2026-09-25 America/Bogota

- **Scope and approval:** The user subsequently requested real DeepSeek and Telegram integration testing, selected dedicated restricted host-only files, limits of two provider requests and two outbound Telegram messages per run, and a provider-side $1 cap when available. They privately created a dedicated bot/key setup, reported `SETUP=PASS`, and marked the cap `verified`. That word is an operator assertion recorded by the script; the script did not query or set a provider-side cap. The user authorized one inbound test message from the selected private account. This extension does not authorize production deployment, a commit, push or PR change.
- **Preparation and checked inputs:** `scripts/hermes/live-smoke.py status` returned `PRIVATE_SOURCE=READY`. A redacted bot preflight showed webhook absent and no pending updates. A new Fedora Cloud 44 disposable VM booted in 14.42 seconds and deployed the same pinned gateway/worker profile in 60.23 seconds. `live-start` mounted the gateway state on guest tmpfs, injected only the dedicated live environment through pinned SSH stdin, and returned `LIVE=READY`; `live-status` reported both services active and `worker_network=none`. Runtime resolution returned `provider=deepseek`, the expected DeepSeek API base URL, and chat-completions mode. The gateway log contained a fresh Telegram connection. No direct service `smoke` was run.
- **G08 first real attempt:** The authorized user sent the exact requested Telegram prompt to the dedicated bot and supplied a screenshot showing the bot's response. Telegram inbound and one outbound error reply occurred. The bot reported DeepSeek HTTP 401 authentication failure, so no model answer was produced. Record G08 as ❌ FAIL, not as end-to-end PASS. The screenshot includes a provider request identifier and a masked key fragment; neither is reproduced in this record. The harness cannot independently enforce or count Hermes-internal retries, so the two-request limit is not claimed proven by this attempt. The operator should check provider usage before a new paid run.
- **Containment and repeatability:** `python3 vm/hermes-disposable.py live-stop` returned `LIVE=STOPPED guest tmpfs cleared; synthetic profile restored`; `live-status` then returned `LIVE=INACTIVE no guest tmpfs profile`. The host's five restricted files remain in place for a deliberate private correction. The exact disposable VM was subsequently guarded-cleaned twice, as recorded below. A narrow `replace-deepseek` command was added to change only the private key, and it requires the operator to affirm that the existing cap status still applies to the new key. Focused replacement tests and Ruff passed; full repository gates are stale until rerun on final inputs.
- **Exact next action:** Receive only the private `KEY=REPLACED` or `STOP:` result from `python3 scripts/hermes/live-smoke.py replace-deepseek`. If replacement succeeds, run the bounded direct `smoke` once to check DeepSeek authentication before asking for a second authorized Telegram inbound message. If it fails, keep the live VM profile inactive and diagnose the specific sanitized status without spending additional requests.

### Live extension continuation on 2026-09-25 America/Bogota

- **Private replacement attempt:** The operator ran `replace-deepseek`, affirmed that the recorded cap status still applies, and reported `KEY=UNCHANGED`. The entered value therefore matched the credential that produced DeepSeek HTTP 401. No additional provider request or Telegram message was made. G05 stays ⬜ NOT_RUN and G08 stays ❌ FAIL. A newly created DeepSeek API key is required before another live attempt; do not retry the unchanged key.
- **Current safe runtime:** A second `live-stop` returned `LIVE=ALREADY_STOPPED`; VM `status` returned `DOMAIN=running` and `STATUS=PASS worker=active gateway=active worker_network=none` before cleanup. Guarded `clean` then returned `CLEAN=PASS` twice; `status` returned `DOMAIN=ABSENT`, and an independent host libvirt and instance-path check confirmed both absent while the signed Fedora base cache remained. The restricted host-only files still exist. No credential values were read into this record.
- **Interim validation:** Focused `tests.test_hermes_live_smoke` passed eight cases, including replacement, no-op rerun, unchanged ancillary files, changed-account refusal and unsafe-file refusal. Focused Ruff, `git diff --check`, and the check-only lint gate passed. After regenerating the source-aware manifest to 392 rows, the full offline gate passed 509 Python tests, 140 ShellSpec examples, 220 guide/deployment checks, the full working-tree secret scan, and manifest verification (`/tmp/hermes-live-offline-current.log`). This record edit is itself a manifest input, so G07 remains 🔁 STALE until the manifest and full gate are refreshed on the final record.
- **Exact next action:** The operator creates a fresh API key in the same capped DeepSeek account, reruns `python3 scripts/hermes/live-smoke.py replace-deepseek` in the private terminal, and reports only its final `KEY=` or `STOP:` line. If `KEY=REPLACED`, run the bounded direct API smoke, recreate the disposable VM with `run`, then make one authorized inbound attempt only after `live-start` and `live-status` pass. Do not send another Telegram prompt until the new key differs and the direct API smoke passes.

### Corrected-key run on 2026-09-25 America/Bogota

- **Private correction and G05:** The operator reported `KEY=REPLACED` after entering a different DeepSeek key in the private terminal. `live-smoke.py status` returned `PRIVATE_SOURCE=READY`. The explicit host-only `smoke` printed `LIMITS=1 DeepSeek POST, 1 outbound Telegram POST, no retries; provider_cap=verified`, then `DEEPSEEK=PASS authenticated bounded response` and `TELEGRAM=PASS one outbound API message; inbound authorization NOT RUN`. Thus G05 is ✅ PASS for the direct service smoke. The $1 cap remains an operator assertion, not a script-verified account setting.
- **Fresh VM and live startup:** The verified Fedora base checksum remained `28680fe5b371a5a82ebf43a31926e086a168e59949d03969c5093e7071f90b7f`; fresh boot took 14.29 seconds and synthetic deployment 57.35 seconds. The first `live-start` stopped at private guest injection with only a generic exit status. Its failure handler cleared the tmpfs and restored the synthetic profile; subsequent `live-status` returned `LIVE=INACTIVE` and `status` returned both services active with `worker_network=none`. A narrow allowlist of non-secret guest failure reasons was added so future failures remain diagnosable without printing SSH stderr; a focused regression passed. An unchanged retry of `live-start` then passed. The initial injection failure's cause remains unknown; do not erase that failure from readiness claims.
- **G08 corrected-key outcome:** Before the user message, `live-status` reported `LIVE=READY STATUS=PASS worker=active gateway=active worker_network=none`; a count-only log query found one fresh Telegram connection and no DeepSeek 401. The authorized user sent the single requested Telegram prompt and reported one bot reply beginning `HERMES_LIVE_OK`, followed by an optional onboarding paragraph. This supports a real DeepSeek/Hermes/Telegram round trip in conjunction with the successful direct DeepSeek check, but the exact-output criterion is ❌ FAIL because extra text was present. The gateway journal did not expose the message body, so the reply content is user-reported evidence, not independently recovered log evidence. No second prompt was sent in this run. The harness still does not independently count Hermes-internal provider retries or verify account usage; the two-request/two-message limit is therefore not certified from gateway logs.
- **Cleanup:** `live-stop` returned `LIVE=STOPPED guest tmpfs cleared; synthetic profile restored`; `live-status` returned `LIVE=INACTIVE`. Guarded `clean` returned `CLEAN=PASS` twice; independent host libvirt and instance-path checks found both exact resources absent, with the verified base cache retained. The five restricted host-only files remain for deliberate future use or `revoke-local`; no provider-side revocation is claimed.
- **G07 and exact next action:** The earlier 509-test offline and lint pass predates the safe diagnostic change and this record, so G07 is 🔁 STALE. Refresh the source-aware manifest and rerun the prescribed full offline and check-only lint gates. The operator is being asked whether the extra onboarding text requires investigation and a further bounded run, or whether the verified core round trip meets the intended live integration check; retain the exact-output failure either way.

### Accepted integration criterion on 2026-09-25 America/Bogota

- **User decision:** The user answered `Round trip is enough` when asked whether to investigate the extra onboarding paragraph. The current G08 criterion is therefore the live, authorized Telegram-to-Hermes-to-DeepSeek-to-Telegram round trip with the test marker present, service isolation maintained, and guest credentials cleared. It is ✅ PASS on the user-reported bot reply and observed guest status/cleanup. The stricter exact-output check that was specified before the message remains a recorded ❌ FAIL in the attempt above; no exact-response fidelity is claimed, and no further paid request is authorized by this decision.
- **Exact next action:** Refresh `docs/migration-manifest.txt` after this record and guide are final; rerun `toolbox/run-offline-checks.sh`, `toolbox/run-check-only-lint.sh`, and `git diff --check`. If all pass, mark G07 and T07 ✅ PASS/DONE and close the extension while reporting the transient first inject failure, unverified gateway-internal request count, operator-asserted cap, and retained host-only test credentials.

### Final-gate correction on 2026-09-25 America/Bogota

- **Preserved failed attempt:** The pre-closure offline run passed 220 guide/deployment checks, 140 ShellSpec examples, 510 reviewed Python tests, and the full working-tree secret scan, but the aggregate gate failed at manifest verification. During that run, the new `scripts/hermes/live-guest.py` was changed from mode 0644 to the repository's required executable mode 0755 after the manifest had been generated. The manifest therefore detected exactly that mode and payload/inventory fingerprint drift. This is a stale manifest input, not a source test failure; G07 remains 🔁 STALE until regeneration and rerun. The complete sanitized run is `/tmp/hermes-live-offline-preclosure.log`.
- **Exact next action:** Regenerate the manifest with the final executable mode, rerun the complete offline and check-only lint gates, and record their actual outcomes. No further live provider or Telegram calls are needed for the user's accepted integration criterion.

## Final closure on 2026-09-25 America/Bogota

- **G07 candidate result:** After correcting the new guest helper to executable mode and refreshing the 392-row source-aware manifest, the prescribed offline gate passed 220 guide/deployment checks, 140 ShellSpec examples, 510 reviewed Python tests, the full working-tree secret scan, and manifest verification (`/tmp/hermes-live-offline-candidate.log`). Check-only lint (`/tmp/hermes-live-lint-candidate.log`) and `git diff --check` passed. The earlier failed stale-manifest attempt remains recorded above.
- **Final-input validation:** This closure entry is itself part of the manifest payload. Regenerate the manifest and repeat the full offline gate, check-only lint and `git diff --check` on this exact record before delivering; if any fail, reopen G07 and T07. Successful results are retained in `/tmp/hermes-live-offline-final.log` and `/tmp/hermes-live-lint-final.log`. The disposable VM and temporary guest live profile are absent by repeated guarded cleanup and independent host checks. Dedicated test files remain restricted on the host; provider-side revocation and actual cap verification are not claimed.
- **Outcome and limits:** G05 confirms one real DeepSeek API completion and one real Telegram outbound API message with no script retries. G08 confirms, under the user's accepted criterion, one authorized inbound message reached the Hermes gateway and yielded the requested marker in the bot reply; extra onboarding text caused the separate strict exact-output check to fail. The first corrected-key `live-start` injection failed transiently and recovered to synthetic mode; its unchanged retry passed, but the cause is not established. Gateway-internal provider retry counts and the operator-asserted $1 cap were not independently verified. No production deployment, commit, push, merge or PR change occurred in this extension.
- **Next action:** No additional test is required for the accepted round-trip criterion. If the dedicated test credentials are no longer needed, the operator can run `python3 scripts/hermes/live-smoke.py revoke-local` privately and separately revoke both credentials at their providers. This is an optional follow-up, not a claimed completed action.

## Delivery authorization on 2026-09-25 America/Bogota

- The user explicitly requested `commit and push` after the live integration result and bare-metal readiness review. This authorizes one commit of the current reviewed lab, cleanup, tests, documentation and manifest changes on `codex/hermes-deepseek-e2e-r1`, followed by a non-force push to its tracked `origin` branch. It does not authorize a merge, PR change, production deployment, or provider-side credential revocation. Earlier no-commit/no-push statements above describe their historical attempts, not this delivery.
- The local branch and live remote branch both pointed to `fb369c3695ec94dc40735bc45b567242920ff93b` before this delivery. The worktree contains the reviewed tracked changes and four new source/test files; no private credential file is in the selected paths. Refresh the source-aware manifest after this note, run the repository offline and check-only lint gates on the final inputs, inspect the staged diff, then commit and push without force. If any gate or hook changes files, refresh the manifest and repeat the affected checks before push.
