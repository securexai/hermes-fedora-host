# Gate 2 temporary unattended access — execution record

Record state: ✅ COMPLETE for the temporary access amendment. This is the current record for the 2026-09-24 access amendment. The earlier source-repository continuation in `mikrotik/docs/plans/2026-09-20-hermes-manual-gate2-lab-plan.md` §9 is historical. The migrated [Gate 2 plan](2026-09-20-hermes-manual-gate2-lab-plan.md) supplies the fixture and phase boundaries; this record owns the temporary access change.

## Approved baseline

- **Outcome:** privately rotate the VM administrator password and close old authenticated sessions; install temporary, root-owned host and guest privilege wrappers limited to the approved Gate 2 VM and credential-free Hermes helpers; verify allowed and rejected commands and sanitized logging; remove grants around shutdown of the retained VM.
- **Target and scope:** `lab-hermes-manual-r1`, UUID `d39e5ca7-aded-457c-b01d-a1817a4725a8`, MAC `52:54:00:94:58:36`, `fvh-nat`. The user expressly selected this VM and credential-free helpers. `lab-hermes-deepseek-e2e-r1`, production, provider and messaging credentials, commits, pushes and PR changes are outside this amendment.
- **Security boundaries:** host and guest sudoers rules are separate; sudo only invokes fixed, validated root-owned wrappers outside user-writable repositories. No sudo password is stored. Any LUKS passphrase, DeepSeek key or Telegram token uses a private prompt.
- **Acceptance:** private password rotation and session termination verified; exact guest and host rules pass `visudo`; installed code has trusted ownership and modes; the intended actions succeed and extra args, unrelated commands and direct helper paths are rejected; actions log without credentials; guest grants are removed before VM shutdown and host grants after, with retained VM shut off.

## Tasks and gates

| ID | Task / dependency | Progress | Gate | Status / evidence |
| --- | --- | --- | --- | --- |
| A1 | Bind to exact VM; none | ✅ DONE | UUID, MAC, network, guest identity | ✅ PASS for live binding recorded in source §9; independent console SSH host-key check D-04 remains ⬜ NOT_RUN |
| A2 | Rotate guest password privately, terminate sessions; A1 | ✅ DONE | Fresh sudo password works and old sessions end | ✅ PASS: QEMU guest agent accepted the private yescrypt password reset; the operator reported `FRESH_SUDO_OK` after `sudo -k && sudo -v`, and read-only SSH showed only the new `aicowork` session and manager. The separate `hermes` service manager remained |
| A3 | Install and validate guest grant; A2 | ✅ DONE | Trusted files, exact rule, `visudo` | ✅ PASS after repair: root-owned rule mode 0440 passed `visudo`; root-owned wrapper mode 0755 matched reviewed SHA256 `1b71febd70518502be5b7387cadbd0c4901e8a5177bd835ecfc86b314aefb109`; `sudo -n -l` listed only the fixed NOPASSWD helper actions |
| A4 | Install and validate host grant; A1 | ✅ DONE | Trusted files, exact rule, `visudo` | ✅ PASS after correction: operator reported `HOST_GRANT_INSTALLED`; installed host wrapper is root-owned mode 0755 and SHA256 matches reviewed source `e778073098d27c9a703816f55458bedcedf3e3d9509e6caeec04a682dfc8f109`; `sudo -n -l` listed only exact status/start/shutdown NOPASSWD actions. Independent root readback confirmed rule owner root, mode 0440, `visudo` PASS, and same hash on an idempotent reinstall |
| A5 | Exercise allowed/rejected actions and sanitized logs; A3, A4 | ✅ DONE | Boundaries hold | ✅ PASS: guest worker-key and host status allowed; four rejected forms on each side required a password after cache clear; both journals recorded action/result without credential material |
| A6 | Remove guest grant, shut down exact retained VM, remove host grant; A5 | ✅ DONE | No temporary grants remain, VM shut off | ✅ PASS: user terminal reported `GUEST_GRANTS_ABSENT`, exact VM `shut off`, then `VM_OFF_ALL_GRANTS_ABSENT`; independent host path and sudo listing checks confirmed host grant absent |
| A7 | Close repository record and migration manifest; A6 | ✅ DONE | Current source-aware manifest and docs | ✅ PASS: final record included in the 368-row manifest; source-aware verification and the full offline gate passed |

## Evidence and corrections

- The source continuation §9 records the initial fixture observations, private password rotation, legacy grant removal, replacement of an exposed lab SSH key, machine-id pin and offline checks. The replacement key is the only authorized SSH key; the old one was proved rejected. No private key or password is copied here.
- 2026-09-24: the operator successfully ran `sudo virsh -c qemu:///system qemu-agent-command lab-hermes-manual-r1 '{"execute":"guest-ping"}'` and received `{"return":{}}`. The first read-only `gate2-guest-recovery.py inspect` attempt failed because `hostnamectl --static` could not access the guest system bus in the agent context. It did not install a rule. The helper now reads `/etc/hostname`; syntax parsing passed. The repeat live inspection passed VM root/hostname/machine-id identity checks and established the exact installed rule state below.
- 2026-09-24: the access amendment was mistakenly developed in the retained `mikrotik` source. Four new helper files were copied into `hermes-fedora-host` without overwriting its existing dirty files. Inspection in the destination found the guest sudoers rule and wrapper named the host account `aicloudopspecial`; both were corrected to VM account `aicowork`. The host account remains `aicloudopspecial`. This correction invalidates source-only offline results and any installed guest wrapper claim.
- 2026-09-24 live read-only inspection: guest agent ran as root on `lab-hermes-manual-r1`; machine-id SHA256 matched the pin. Legacy broad rule and one-use reset rule were absent. `/etc/sudoers.d/90-hermes-gate2-test-guest` was root-owned, mode 0440 and passed `visudo`, but its command list named `aicloudopspecial`. The installed wrapper was root-owned, mode 0755; its content and bundle identity are not yet verified. This is a failed account-boundary gate, not a working grant. No password was entered into or printed by the inspection.
- 2026-09-24 one-use guest reset attempt: exact-VM identity checks passed, then `mktemp` under the root-owned libexec directory failed `Permission denied` before it created a helper or sudoers rule. The VM reports SELinux enforcing; this record does not attribute the denial to a specific policy rule without AVC evidence. Repeating that file-write method unchanged is rejected. The recovery helper now offers `reset-password`: a private terminal prompt, local yescrypt hash using `mkpasswd -s`, and QEMU `guest-set-user-password` through the in-process libvirt API. Only the hash is sent; no raw password is placed in arguments, environment, files or this record. Python AST, CLI help, synthetic yescrypt generation, a mocked libvirt reset (verified exact command, `crypted=true`, target user and absence of raw password in payload), and `git diff --check` passed after the change. At that point the live password change was ⬜ NOT_RUN; a later attempt succeeded below.
- 2026-09-24 first live `reset-password` attempt: exact VM and guest identity checks reached the private prompt, then local length validation rejected the chosen password with `choose 16 to 256 characters; no change made`. The guest-agent password command was not called; the VM password remains in its prior unknown state. The retry uses the same command with a privately chosen password of 16–256 characters. No password value or hash was recorded.
- 2026-09-24 second live `reset-password` attempt returned `VM_PASSWORD_CHANGE_ACCEPTED; verify with fresh sudo authentication`. This means QEMU guest agent accepted the yescrypt hash for `aicowork`; it is not yet proof that the new password authenticates through sudo. A credential-free launcher at `/tmp/hermes-verify-vm-password.sh` was syntax-checked (SHA256 `ed4d153146351c7fcd78759a82971b4709898c9748348bc2a01a627fb539ae55`). It uses the pinned replacement SSH key, runs `sudo -k`, privately prompts for the new VM password via `sudo -v`, prints `FRESH_SUDO_OK` only on success, then calls `sudo loginctl terminate-user aicowork`. The expected SSH disconnect is part of session termination.
- 2026-09-24 guest repair staging: read-only SSH reported only the fresh `aicowork` session (55) and its manager (56), plus the preexisting `hermes` service manager. The installed guest wrapper is root-owned mode 0755 and hashes to `d25930f52cd369d46a86a43ff21cd9304591ca5705cf7201e6e3157342264ae8`, different from the corrected destination wrapper `1b71febd70518502be5b7387cadbd0c4901e8a5177bd835ecfc86b314aefb109`. The installed bundle has root-only files, so an unprivileged full diff is unavailable. The repair bundle was built from the migrated `hermes/hermes-repository-migration` manual tree with only the three new Gate 2 helpers overlaid; this excludes unrelated in-progress DeepSeek edits. Local and guest staged trees both passed `gate2-test-grants.sh --check` and share canonical file-manifest SHA256 `a3230afbc4990bc61a29247a422de7ac34b5c1c1681b65e55ee8a55beab13998`. The staged guest path is `/home/aicowork/hermes-gate2-approved-20260924`; installed root-owned code has not been replaced yet.
- 2026-09-24 repair launchers: `/tmp/hermes-repair-guest.sh` passed `bash -n` and has SHA256 `b42ecf9bfbc1077c33471e92a94e8b99fe006bd864aec1fc59d9154b031a1fc2`; it was staged as `/home/aicowork/hermes-gate2-repair.sh` (mode 0700) with matching readback hash. It checks guest identity and staged source fingerprint, validates source/sudoers, clears sudo cache and prompts privately, removes only the old named grant/bundle, installs the corrected root-owned bundle with pinned machine-id, then runs `visudo` and ownership checks. The workstation launcher `/tmp/hermes-repair-guest-launch.sh` passed `bash -n` (SHA256 `600d85431fe303f3702f1b7a8837cb7dbdb622c9ea6d0a8255fcd72109b71405`). No repair has run yet.
- 2026-09-24 guest runtime gate: operator repair output showed `/etc/sudoers.d/90-hermes-gate2-test-guest: parsed OK`, root/mode 0440 rule, root/mode 0755 wrapper and `GUEST_GRANT_REPAIRED`. Fresh SSH `sudo -n -l` listed the `aicowork` NOPASSWD wrapper actions. Installed wrapper SHA256 matched the corrected source. After `sudo -k`, `sudo -n true`, extra argument, unknown action and direct bundle helper all returned 1 with `sudo: a password is required`; this is the expected wheel fallback without a cached password. The allowed `worker-key` action returned 0, generated a key on first use, and returned 0 on rerun while preserving its public fingerprint `SHA256:kfOTfrGH6+SfVDQTyJarZobYj1Hz/8cHw9UYl/9/ZLw`. `journalctl -t hermes-gate2-test` showed guest action start/success records without credential text. No M02–M04 deployment actions were run.
- 2026-09-24 host install attempt stopped at `STOP: untrusted parent /usr/local` before creating a wrapper or sudoers rule. Outside the sandbox, `namei`/`stat` showed the Kinoite host has root-owned `/usr/local -> ../var/usrlocal` and root-owned mode-0755 `/var/usrlocal/libexec`; the guest uses a real `/usr/local`. The installer now has separate `HOST_BASE=/var/usrlocal/libexec/hermes-gate2-test` and `GUEST_BASE=/usr/local/libexec/hermes-gate2-test`, validates the respective parent chain and writes different exact sudoers paths. The host wrapper comment was corrected. `gate2-test-grants.sh --check`, wrapper `bash -n` and `git diff --check` passed after the change. No new host grant has been observed installed yet.
- 2026-09-24 host runtime gate: operator reported `HOST_GRANT_INSTALLED`. Read-only `stat` found root-owned mode-0755 `/var/usrlocal/libexec/hermes-gate2-test/gate2-test-host.sh`, SHA256 `e778073098d27c9a703816f55458bedcedf3e3d9509e6caeec04a682dfc8f109`, identical to source. `sudo -n -l` listed the exact status/start/shutdown host actions. Allowed `sudo -n ... status` returned `domain=lab-hermes-manual-r1 state=running`. After `sudo -k`, unrelated `true`, extra argument, unknown action and direct `virsh` all returned 1 with `sudo: a password is required`. The host journal recorded grant installation and status start/success without credential text. The installer validates its sudoers file with `visudo`; independent root readback is still pending.
- 2026-09-24 destination offline gate: a sandboxed Toolbox call failed to access Podman, so it was repeated with host access. Manual profile 395/0, review regressions 142/0, backup 43/0, configure 64/0, contract 30/0, M01 9/0, clean-install rehearsal, guide 220/0, deployment specs 140 examples/0, Python 486/0, and full-tree secret scan passed. The aggregate gate exited 1 at migration-manifest verification because the destination payload has five new files and preexisting DeepSeek edits, while the source-repository handoff changed its dirty-input identity. This manifest is 🔁 STALE, not accepted. Refresh after the access record and cleanup stabilize, then rerun its verification.
- 2026-09-24 final cleanup prepared, not executed: `/tmp/hermes-gate2-guest-cleanup.sh` passed `bash -n`, SHA256 `1cd3ae1ec43c8274b5551c111f7c044a1ac68af29ef6975c7f746f1ef3ac8472`, and its guest copy under `/home/aicowork/` matched mode 0700. It checks guest identity and staged source fingerprint, uses a private VM sudo prompt, removes only named guest grant/bundle paths, then checks their absence. `/tmp/hermes-gate2-final-cleanup.sh` passed `bash -n`, SHA256 `f53103e0771647e58289b69c5f4564f0f45c36fce9941ce0c8139c6ca10b68a3`. It uses a private workstation sudo prompt for independent host rule `stat`/`visudo` and an idempotent installer rerun, calls the guest cleanup over pinned SSH, uses only the fixed host wrapper for graceful shutdown, removes the host grant after shutoff, then checks both host grants absent and exact VM off. No password is stored or logged.
- 2026-09-24 final cleanup result: the operator's private terminal first rejected one workstation sudo password attempt, then accepted the retry. Independent host sudoers readback showed root/mode 0440 and `visudo` PASS; the idempotent host install rerun preserved the rule hash and passed `visudo` again (`HOST_RULE_VERIFIED`). The private VM sudo step returned `GUEST_GRANTS_ABSENT`. The fixed host wrapper then gracefully shut down only `lab-hermes-manual-r1`, returned `domain=lab-hermes-manual-r1 state=shut off`, and the host removal plus exact-VM state check returned `VM_OFF_ALL_GRANTS_ABSENT`. A later independent unprivileged host check found the old/new host sudoers paths and installed host wrapper absent; `sudo -n -l` again required a password. Guest removal was checked as root before shutdown; guest state cannot be re-inspected by SSH while off. The domain and disk were retained. No raw password was recorded.
- 2026-09-24 repository closure: the source-aware manifest generator wrote 368 rows and 13 excluded candidates, then `--verify` passed against the current source/toolbox/companion checkouts. `git diff --check` passed. This record closure changes the payload once more, so the manifest is regenerated and verified immediately afterward; the final manifest and payload hashes belong in the delivery response, avoiding a self-referential hash in this included record. The local `/tmp` verification, repair and cleanup launchers and staged build tree were removed. The off VM retains user-owned, credential-free staged source and the VM-local worker client key for later Gate 2 work; neither is a sudo grant.
- The destination already has unrelated in-progress DeepSeek changes on `codex/hermes-deepseek-e2e-r1`; preserve them. `bash scripts/hermes/manual/lab/gate2-test-grants.sh --check` passed after the guest-account correction; it validated source syntax and both generated sudoers rules with `visudo`. Python AST parsing of the recovery helper and `git diff --check` passed. These are source checks, not installed-grant evidence. Migration manifest and payload identities are stale after adding these sources; refresh only once the access amendment is stable.

**Current outcome:** VM password rotation, old-session termination, separate guest/host grant validation, allowed/rejected checks, sanitized logging and final cleanup passed. The retained VM is shut off and named temporary grants are absent. The destination manifest is current after final verification. D-04 independent console SSH host-key verification remains open; M02–M04 deployment was not run.

**Exact next action:** no further temporary-access action. Before any later Gate 2 deployment, independently verify the VM SSH host key at its console (D-04), obtain separate authorization for the phase, and reassess whether temporary grants are actually needed. Keep the retained VM off until then.
